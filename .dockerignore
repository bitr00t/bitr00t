.git
.github
.vscode
.idea
node_modules
.next
npm-debug.log
README.md

# Secrets never belong in a build context.
.env
.env.*
!.env.example

# content/ is deliberately NOT excluded. The articles are checked out there
# and the build needs them; excluding it produces an image that builds
# cleanly and serves nothing.
